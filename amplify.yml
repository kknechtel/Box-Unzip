version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Using Docker container"
    build:
      commands: []
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
